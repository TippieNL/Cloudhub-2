<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        Progress while files upload, so a big video can be watched finishing
        from the home screen. A runtime permission on Android 13+, asked for
        when an upload is first queued rather than at launch, and refusing it
        costs only the notification: the in-app tracker is the same either way.

        Deliberately NOT a foreground service: that would need
        FOREGROUND_SERVICE_DATA_SYNC and a service type as well, to buy
        expedited scheduling this app does not need.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      No permissions beyond the network, deliberately.

      CAMERA: the camera app takes the photo or the clip through the capture
      intent and holds its own permissions; declaring CAMERA here would force a
      prompt that buys nothing. RECORD_AUDIO likewise for video.

      READ_MEDIA_IMAGES / READ_MEDIA_VIDEO: the system photo picker returns
      only what the user chose, so reading the gallery wholesale is never
      needed.

      Storage: downloads go through MediaStore, which grants an app writes to
      its own Downloads entries.
    -->

    <application
        android:name=".CloudHubApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/CloudHubTheme"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- CloudHub in the Android share sheet. Shared files join the
                 same resumable upload queue as anything picked in the app. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="text/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity>

        <!-- Gives the camera app a writable URI for the photo or clip it is
             about to record, without exposing app storage to anything else. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
