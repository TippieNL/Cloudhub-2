<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything, so the platform theme only has to stay out
         of the way: no action bar, and a window background matching the app's
         own so there is no flash of white on launch. -->
    <style name="CloudHubTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#F7F7F8</item>
        <item name="android:statusBarColor">#1479C9</item>
    </style>
</resources>
